# Shanmen Unreal 0.0.9B

```text
project_id = Dev.D.UE.0.0.9B
active_root = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B
active_uproject = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\demo_map.uproject
engine_root = C:\Program Files\Epic Games\UE_5.8
current_task = Dev.D.UE.0.0.9BFix.P3
current_status = READY_FOR_0_0_9BFIX_P4_OR_F_PLANNING
prompt_directory = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Docs\Prompt
report_directory = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Docs\Report
version_report = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Dev.D.UE-0.0.9B.codex.report.md
current_prompt = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Docs\Prompt\Dev.D.UE.0.0.9BFix.P3_prompt.md
current_report = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Docs\Report\Dev.D.UE.0.0.9BFix.P3_report.md
formal_P_stage_started = true
code_B_source = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Source\demo_map\CodeB
p2_source = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Source\demo_map\CodeB
p2_automation_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\Dev.D.UE.0.0.9B.P2.0.r0_automation.log
p2_smoke_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\Dev.D.UE.0.0.9B.P2.0.r0_smoke.log
p3_source = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Source\demo_map\CodeB
p3_automation_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\Dev.D.UE.0.0.9B.P3.0.r0_all_codeb_automation.log
p3_smoke_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\Dev.D.UE.0.0.9B.P3.0.r0_default_map_smoke.log
p3_screenshots = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\P3Screenshots
p4_source = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Source\demo_map\CodeB
p4_automation_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\Dev.D.UE.0.0.9B.P4.0.r1_all_codeb_automation.log
p4_smoke_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\Dev.D.UE.0.0.9B.P4.0.r1_default_map_smoke.log
p4_screenshots = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\P4Screenshots
p4x_automation_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\P4x.r2.Automation.CodeB.current.log
p4x_direct_start_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\P4x.r2.ProductStart.Normal.current.log
p4x_profile_settlement_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\P4x.r2.Automation.ProfileSettlement.current.log
p4x_profile_session13_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\P4x.r2.Automation.ProfileSession.current.log
p4x_real_input_1280_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\P4x.r2.RealInput.1280x720.current.log
p4x_real_input_1920_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\P4x.r2.RealInput.1920x1080.current.log
p4x_default_map_smoke_log = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B\Saved\Logs\P4x.r2.DefaultMapNoCodeBHost.current.log
quarantine_root = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9B_QUARANTINE_I0R0_FROM_0.0.9-XFix1_20260804T194204
requested_source = CANCELLED_BY_USER_DECISION_2026-08-04
actual_baseline_source = C:\AIDev\shanmen-ue\Dev.D.UE.0.0.9-XFix1
```

0.0.9BFix.P3：将宗门 UI Host 的唯一出战请求收敛为 `StartAttemptId` 关联的 `Coordinator → M01RuntimeAdapter → RuntimeReady` 链。Adapter 仅验证正式 M01 descriptor／World／GameMode／WorldSettings／Controller／Pawn／GameOnly 输入的当前事实，接受既有 M01 materialization 请求后才报告同 Attempt 的 RuntimeReady；旧、重复或不匹配回执不改变状态。Coordinator 在 `RuntimeReady → InRun` 后才调用 selection-aware P5→P6 observer；其拒绝仅是审计，不能伪装为 activation failure。TechnicalStartFailure 只释放匹配 attempt 的 transient state、回滚尚未确认的 Code A prepared run、复核无 P6 session 后返回 AtSect，并清除活动 Run identity；P5 未移动／锁定、P8 零写入。P3 未修改 `demo_mapGameMode`／`demo_mapPlayerController`、地图、Actor、战斗、库存或终局权威。2026-08-08 指定 Editor 和 Game Development 两个原生命令均以 exit code `0`、UBT `Succeeded` 完成；未启动产品、CTA、PIE、截图、自动化、Smoke、回归、Cook、Package 或最终验收。状态：`READY_FOR_0_0_9BFIX_P4_OR_F_PLANNING`。

P16.0.r0：P9 `CodeB.NormalContainer.BasicCache` 与 P11 `CodeB.BodyContainer.BasicCorpse` 的未物质化首创路径改为 Code B 独占的确定性加权 Loot Profile。`CodeB.LootProfile.BasicCache.r1`（SpiritDust 权重 3、数量 2--3；IronShard 权重 2、数量 1--2）和 `CodeB.LootProfile.BasicCorpse.r1`（IronShard 权重 3、数量 1--2；SpiritDust 权重 2、数量 1）各有一个固定必出 group。roll 仅由精确 Owner/Run/target/definition、尸体的 DeathReceipt、Profile id/version/digest 和固定 `CodeB.DeterministicWeightedLoot.Crc32.r1` 导出；无时钟、Actor、UI、临时 GUID 或全局 RNG。P9/P11 同一 Owner durable replacement 只保存一次完整 P1 root/item 图、Hidden 状态、原 receipt/digest 和 P16 Profile provenance/result digest；重放读回既有记录，失败零写入。固定 recipe 仍仅用于验证历史 materialized records，未被改写。P10--P15、P5/P6、P8 与 Code A 未修改。2026-08-07 `demo_mapEditor Win64 Development` 编译 exit `0`、UBT `Succeeded`；真实随机性、重开、死亡/打开重放、转移、恢复、结局、自动化、截图、Smoke、Game Build、Cook、Package 与最终验收留给 `0.0.9B.F`。状态：`READY_FOR_NEXT_P_FUNCTIONAL_WITH_F_DEBT`。

P18.0.r0：未来首次物质化的唯一 `CodeB.NormalContainer.BasicCache` 改用 `CodeB.LootProfile.BasicCache.r2`；已 materialized 的 `BasicCache.r1` 与 `BasicCorpse.r1` 按 receipt 的 ProfileId／Version／digest 回读，原图与历史 digest 不改写。r2 原样保留 `Guaranteed.Main`（SpiritDust weight 3、qty 2--3；IronShard weight 2、qty 1--2），追加 `Optional.SpatialUtility`：独立 `NoDrop:9 / Spawn:1` gate 命中后按 WindTalisman weight 1、BackpackLevel1 weight 3 选一件 qty 1。确定性 identity 为 OwnerId、RunInstanceId、BasicCache target、source DefinitionId、Profile id/version/digest 与 CRC32 r2 algorithm；没有时钟、Actor、UI、临时 GUID、旧 Loot 或全局 RNG。命中时在同一个 Owner durable replacement 内写入 BasicCache root 的 parent 与由 P17 canonical Definition resolver 导出的稳定、空、唯一 ChildContainer；无命中仅保留主材料，非法候选／容量／child 图零写入。P10 既有复合 P1 transaction 保持 UI/输入/target identity 不变，只在提交前拒绝 Hidden/Searching、parent-only、child mutation、clone/new id 或 P6 之外 destination；成功以原 ItemId 与 child graph 同时从 P9 转入 exact P6。P5/P6/P7/P8、P11/P12、P13/P14/P15 与 Code A 权威未改。2026-08-08 `demo_mapEditor Win64 Development` 编译 native exit `0`、UBT `Succeeded`；真实 roll、转移、P7、P8、recovery、自动化、截图、Smoke、Game Build、Cook、Package 与最终验收均留给 `0.0.9B.F`。状态：`READY_FOR_NEXT_P_FUNCTIONAL_WITH_F_DEBT`。

P19.0.r0：P14 仍以 schema 4 的一个 `WorldDrop` record、派生 world-container 和同一 root `ItemId` 表示地面图；原有 simple BaseQuick→world→空 BaseQuick 分支保持不变。仅 `WindTalisman` 与 `BackpackLevel1` 取得附加 whole-graph 分支：Store 在一次 Owner durable replacement 中验证稳定 `SpatialChildGuid`、正式 child type/capacity、普通 child contents 与一层限制，允许非空 child 但拒绝其它复杂 parent、child 单独操作、clone、flatten 或新 identity。P7 的 `GroundDropZone NativeOnDrop` 复核实际来源 cell；WorldDropTarget 只显示 root，并只允许真拖拽回空 BaseQuick 或匹配的空装备栏。P8 player-only closure 现在移除 world root、child container 和全部 child item，防止 Extracted／Dead／RecoveredAbandon 向 P5 泄漏；P13 仍只 reconcile，P15 与 Code A 权威不变。2026-08-08 指定 `demo_mapEditor Win64 Development` 编译 native exit `0`、UBT `Succeeded`；真实空／非空图往返、Actor、重开、rebind、终局、自动化、截图、Smoke、Game Build、Cook、Package 与最终验收留给 `0.0.9B.F`。状态：`READY_FOR_NEXT_P_FUNCTIONAL_WITH_F_DEBT`。

P15.0.r0：在既有 P13 九槽引用之上接入唯一的 `RestoreHealth` 快捷使用链。Code A 只将离散 1--9 按下转给 Manager；正常 `RunActive`、存活、未满血、无库存/容器 UI 的精确 OwnerId/RunId 才可调用 Code B。Code B 复核 P6 committed session、P13 binding、BaseQuick 简单物品、数量与定义 descriptor，一次 Owner-document 原子替换扣同一 ItemId 一单位、归零删除、reconcile binding、写 Pending receipt。P6 schema `4→5` 保存无库存镜像的稳定 owner/run/ordinal receipt；Code A health 仅记当前 pawn/run 已处理 ReceiptId、capped restore 后 ACK，失败保持 Pending 供正常游戏内最小重试。P8 terminal 丢弃 ledger，现存 P6 graph 仍遵循 Extracted/Dead/RecoveredAbandon；P5 schema `7→8` 仅序列化 definition 的 effect descriptor，历史数据只从既有 consumable `HealAmount` 安全迁移。最终 `demo_mapEditor Win64 Development` 编译 exit `0`、UBT `Succeeded`；未运行真实功能测试，全部留至 `0.0.9B.F`。状态：`READY_FOR_NEXT_P_FUNCTIONAL_WITH_F_DEBT`。

0.0.9B 的产品方向是统一的单格物品、容器、仓库、装备、搜索、拖拽与撤离结算框架。整体工程继承 XFix1 的地图、战斗、Run、撤离、输入、资产和工具；新背包相关正式能力归入代码 B。

P2.0.r0 已完成 Code B 玩家配置与仓库/装备/空间道具内部容器的非运行时 vertical slice：固定 fixture 包含 30 格仓库、6 格基础储存、武器/护甲/空间装备槽、2 个饰品槽及空间道具内部 4 格容器；新增只读 Projection 与单一 Application Service/Command Adapter，所有变更继续落到 P1 Repository 事务。共 23 个自动化测试（P1 4 + P2 19）全部通过，固定命令序列重放 100 轮通过。

P3.0.r0 已在上述只读 Projection / Application Service 边界上增加真实 C++ UMG/Slate 页面和默认关闭的独立 UI Host。页面显示玩家兵器、道袍、双饰品、空间道具、基础 6 格、局外仓库 30 格、空间储物门控、物品详情与 1–9 个“未接入”占位快捷栏；展示层只保留稳定容器/槽位地址和 ItemId，所有写入仍经 P2 Service → P1 Repository。`CodeB.P3.Open`、`CodeB.P3.Close`、`CodeB.P3.Reset` 是唯一开发入口，未写入正式 Code A 地图、运行时玩家、Profile、Run 或 SaveGame。

代码 A 仍是默认地图正式运行时路径；P3 Host 默认关闭且只在显式打开时创建 fixture/service/root。最终全量 Code B 自动化 47/47 通过（P1 4 + P2 19 + P3 24），Editor/Game Win64 Development 构建和默认地图 Smoke 通过。下一任务只允许按策划部新 Prompt 单独下发；建议为 Code B 的拖拽与快速动作，当前不自动启动。

P4.0.r0 的 Controller 自动化 57/57 通过，但真实 UMG Cell 的点击／双击没有被证明，故其结论保留为 `P4_NEEDS_REWORK`。P4.0.r1 修复了输入层：外层 `UCodeBP3CellButton` 是每条手势唯一、可命中的 pointer owner；内层 `UButton` 仅作 `HitTestInvisible` 视觉面。左键在该 Cell 上注册 DetectDrag，右键只开菜单，MouseUp/DoubleClick 通过页面级稳定 ItemId 时间窗去重，任一物理手势最多提交一次命令。`UCodeBP4DragOperation` 自身处理 Slate Escape 取消，清除 Preview／临时状态而不触及 P1。

P4.0.r1 的 `CodeB.P4.RunRealInputTrace [width] [height] [Quit]` 在实际 viewport 挂载的生产 Host 中，以 Cell/Control 的真实屏幕坐标调用 `FSlateApplication` 命中路由；没有直调任何 Widget NativeOn、P3/P4 Controller 或 P2 Service。它记录 `P4.UITrace`（Gesture、稳定地址、DragOperation、Enter/Leave、Preview、命令数、Revision）并验证选择、Move、Merge、Swap、Equip、Replacement、Unequip、空间内部移动、Loaded Spatial／类型／同源拒绝、双击／右键菜单 QuickMove、Escape、Close/Reopen。1280×720 与 1920×1080 全矩阵均通过；日志为 `Saved\Logs\Dev.D.UE.0.0.9B.P4.0.r1_real_input_1280.log` 与 `..._1920.log`。全量 Code B 57/57、ProfileSettlement 19/19、ProfileSession.13、Editor/Game Development 构建和默认地图未 Open Host Smoke 均通过；P4 仍不接管正式 Code A Runtime、Profile、Run、Loot 或 SaveGame，且不自动进入 P5。

P4x.0.r0 在 P4.0.r1 通过后执行。正式传送页仍使用既有 Profile/Runtime/地图启动链，但 `Ademo_mapV3ProgressionManager::StartPreparedProfileRun` 直接调用 `Fdemo_mapProfilePreparationFlow::StartPreparedRunDirect`，后者调用 `Udemo_mapProfileSessionSubsystem::StartRunWithoutPreparation`。该请求不读取、恢复、校验或重置旧 `PreparationLayout`；底层 Profile 验证把它作为仅供历史工具保留的透明元数据。自动化以失效旧武器／空间戒指 ID、非标准旧 Hotbar 长度、无装备、从未创建 Widget 的 Profile 证明可连续 Start Run 两次且没有隐式部署物品。默认地图 Smoke 也确认未创建 Code B Host、fixture、Repository 或写入。

Code B 的 P4x 契约为：仓库、基础 6 格、空间戒指快捷内部格和空间储物囊非快捷内部格都是普通储物位置；只有兵器、道袍、双饰品和空间戒指装备栏保留类别／单格规则。真实拖拽是 Move、Swap、Merge、Replacement 和 Unequip 的唯一用户写入入口；双击、右键、详情和条件式“装备／卸下”仅选择、查看或提示明确拖放目标。不存在 Move/Merge/Swap/QuickMove/放回按钮或隐藏 Console 成功写入捕获路径。空间戒指 `Spatial.Ring` 仅能装备并显示 `QuickSpatial`；空间储物囊 `Spatial.Pouch` 是普通物品，稳定关联 `PouchInternal`，无快捷／热键语义。二者均保持 Child ContainerId、唯一 ItemId 和 LoadedSpatial 原子拒绝。

P4x 最终证据：Code B `58/58`、ProfileSettlement `19/19`、ProfileSession.13 `1/1`、P4x Direct Start `1/1`、1280×720 与 1920×1080 的真实 `P4.RealInputTrace`、Editor/Game Win64 Development 构建和默认地图未 Open Host Smoke 全部通过。P4x 未让 Code B 接管 Code A 的正式 Profile、Run、SaveGame、Loot、地图、玩家 Actor 或默认 UI；不自动进入 P5。报告中的唯一非阻断项是历史 P4 截图不能作为 P4x UI 证据，验收改以当前真实输入 trace 与逐文件代码审查为准。

P5.0.r0 已接入以正式 Profile `OwnerId` 为隔离键的 Code B 局外 sidecar：首次正常宗门“仓库／人物配置”入口会在无活动 Run 时建立或恢复版本化 snapshot、receipt 与原子备份；每次已接受的 Code B P1/P2 事务才会持久化。旧 Profile 的可读兼容来源保留最小审计摘要和旧实例映射，不回写或镜像 Code A 清单；可选的旧空间父引用会迁移为 Code B 的稳定 `ChildContainerId`，失效关系记入 receipt 而不会伪造物品。P5 不接管 Run、地图、玩家、Loot、搜索、撤离、结算或 Run Save。根据用户 2026-08-06 的阶段安排，P5 的全量回归、可见验证和验收矩阵统一留待 `0.0.9B.F`，因此当前状态为 `NEEDS_PLANNER_DECISION`，不能视作 P5 完整验收。

P6.0.r0 建立 P5 Owner sidecar 内独立的只读 `FCodeBRunInventorySession` 子记录；r1 首次让真实 `SectTeleportCTA → Code A Start → world activation → post-activation observer → bridge` 进入该记录，但 Prepared recovery 曾直接重放 observer；r2 如实证明第二次真实 CTA 会获得新 RunId 并被旧 session 拒绝。P6.0.r3 最小改为同 Owner 已验证 Prepared receipt 的安全新 RunId 重绑：receipt 保留稳定 `ReceiptId`、不可变 `OriginRunId`、原 P5 revision、payload digest、全 item/container/child-container 图；只有 Code A 已提交 `RecoveredAbandon` 的只读上下文、当前 binding 精确匹配、且新 RunId 不同的正常 observer 才可追加 `Old → New` history 并最终单次提交。已提交/普通活跃 session、损坏 graph/digest/source revision、缺失或不匹配上下文都拒绝。P6 r3 不写 Code A lifecycle、Player、Loot、搜索、结算、Run Save 或旧库存；P7 仍只读投影，P8 才定义回收/结算。用户最新裁决：所有 P 阶段仅功能开发与代码审查，真实 CTA、wrapper 执行、全量回归、可见矩阵、Game 构建、截图与最终验收统一归入 `0.0.9B.F`；因此 r3 当前为 `NEEDS_PLANNER_DECISION`，不能当作运行验收。

P6.0.r4 只完成 r3 最后一次源码修改后的 `demo_mapEditor Win64 Development` 编译收尾：2026-08-06 的目标编译退出码为 `0`，UBT 结果为 `Succeeded`，无源码修正。静态审查确认 rebind 仍以 Code A 已完成的 `RecoveredAbandon` 只读 context 为门槛，receipt identity/origin/payload/recovery history 留在 Code B sidecar，且 Code A lifecycle、Run、Player、Loot、搜索、结算、Run Save 与旧库存权威未移动。P6 已可交接后续 P 阶段功能开发；全部真实运行与最终验证债务仍只属于 `0.0.9B.F`。

P7.0.r0 已在 P6 已提交活动会话之上接入生产可达的 Code B 个人背包：现有库存输入在 Profile Flow 的 `RunActive` 阶段先以当前 `OwnerId + RunInstanceId` 精确查询 P6，会话缺失／不匹配时不创建磁盘 sidecar、P5 迁移、fixture 或 Repository，并非阻塞地保留 Code A 原库存路径。成功时才建立内存 P1 view，复用已挂载 P3/P4 Cell、详情、DragOperation 与 Drop；活动页面隐藏局外仓库，展示装备、基础 6 格、已装备空间戒指快捷空间、空间储物囊非快捷内部格与禁用 1—9。活动作用域封闭旧的点击／菜单命令，唯一持久化路径为真实 `NativeOnDrop → P4 → P3 → P2 → P1 → P6 session`；每次已接受的 P1 事务仅提交一次 P6 session snapshot 与 revision。为使初始 P6 receipt digest 在 Run 内位置变化后仍不可变，首次 P7 提交冻结原携行 item graph 作为 receipt evidence；P5 snapshot、P6 bridge 与 RecoveredAbandon rebind 均未改动。2026-08-06 `demo_mapEditor Win64 Development` 最终目标编译 exit code `0`、`Succeeded`。P7 的真实运行、CTA、自动化、回归、截图、Smoke、Game Build、Cook、Package 与最终验收仍完全归 `0.0.9B.F`。

P8.0.r0 在同一 Owner 的既有 Code B durable record 内加入 terminal receipt history；receipt 冻结 P7 当前 P6 graph 与 layout digest，并由现有 temp-verify-backup-replace 原子保存。仅在 Code A Runtime 结算及 Profile 结算都成功后，或启动恢复的 `RecoveredAbandon` 已被 Code A 持久化后，最小 observer 才以精确 `OwnerId + RunInstanceId + terminal` 通知 Code B，且调用结果不会改变 Code A 返回值或生命周期。`Extracted` 在一次记录提交内将当前 P6 根物品和完整 ChildContainer 图以原 ItemId／数量／位置／ContainerId 合回 P5，保留未出战仓库；`Dead` 与匹配的 `RecoveredAbandon` 冻结同一图后没收，不生成奖励或世界物品。提交同时清除 P6 active session，释放 P5 lock；重复同 terminal 幂等确认，冲突／失配／Prepared／未知通知不创建、迁移、rebind 或写入。P8 开始时关闭匹配 P7 Host，仅清理 UI/Drag/Preview。2026-08-06 `demo_mapEditor Win64 Development` 编译 exit code `0`、`Succeeded`。真实运行、CTA、自动化、回归、截图、Smoke、Game Build、Cook、Package 与最终验收继续只属于 `0.0.9B.F`。

P9.0.r0 新增同 Owner durable record 内、与 P5 局外 snapshot 和 P6/P7 玩家携带 snapshot 分离的 `RunLocalNormalContainers`。其唯一创建服务需要精确 `OwnerId + committed Active RunInstanceId + SearchTargetId + DefinitionId`；首个生产定义 `CodeB.NormalContainer.BasicCache` 使用正式全局 `SpiritDust`／`IronShard` 定义，生成确定性根 Container、真实 ItemId 图、全 Hidden reveal states、receipt／definition digest／materialization digest，并与同一 record 原子保存。相同 target/definition 无写入返回，异定义 target 冲突、缺失／Prepared／terminal／失配会话、未知定义及非法 identity 都不创建、迁移、rebind 或写入；只读 projection 不会隐式 materialize、open、reveal 或触及 P7/P6 玩家物品。P8 terminal finalizer 在关闭匹配 session 时显式丢弃所有 Run-local 普通容器，且仅冻结／合并 P6 玩家携带图。P9 未修改 Code A、P7 UI/Input/Drop 或 P6 bridge。P9.0.r1 复核后没有功能源码修正；`demo_mapEditor Win64 Development` 的同步原生编译于 2026-08-07 以 exit code `0` 完成，UBT 结果 `Succeeded`（目标最新、0 actions）。因此 P9 为 `READY_FOR_P10_INTERACTION_FUNCTIONAL_WITH_F_DEBT`。未执行任何真实运行、CTA、自动化、回归、截图、Smoke、Game Build、Cook、Package 或最终验收，均仍属于 `0.0.9B.F`。

P10.0.r0 将唯一生产目标 `M01.CodeBNormalContainer.BasicCache.01` 接入既有 Code A 交互／距离／Run 入口与 Code B P9 真相：map adapter 只传递静态身份，P9 schema 1→2 和 Owner record schema 3→4 为 Opening／Searching 增加可恢复的 action identity；Open 复用无写入，取消／距离／destroy／terminal 以精确 Owner／Run／target／definition 中断。P7 的生产 P3/P4 Host 以 transient `NormalContainerTarget` 显示目标第二栏；Hidden／Searching 不暴露 ItemId、定义、数量或详情，也不能拖拽，Revealed 回到普通细节与真实 Drop。唯一写链仍为 `NativeOnDrop → P4 → P3 → P2 → P1`，其最终 commit 在同一 Owner 文件原子替换 P6 carry 与 P9 target，并拒绝隐藏／搜索中物品离开目标。M01 adapter 绑定静态锚点且不影响 Code A Run 启动；P8 仍在 terminal 时仅处理 P6、清除 P9。2026-08-07 仅执行一次允许的 `demo_mapEditor Win64 Development` 编译，UBT `Succeeded`、28.28 秒；未运行 Game、CTA、自动化、回归、截图、Smoke、Cook、Package 或最终验收，全部继续属于 `0.0.9B.F`。P10 状态为 `READY_FOR_NEXT_P_FUNCTIONAL_WITH_F_DEBT`。

P11.0.r0 为静态生产绑定 `M01.Encounter.LOW.Skirmisher.01`／`M01.Spawn.LOW.Skirmisher.01`（spawn ordinal `0`）建立独立 `CodeB.BodyContainer.BasicCorpse` Run-local 真值。`BodyTargetId` 和 `DeathReceiptId` 均只从静态 map identity、精确 OwnerId、已 committed RunInstanceId、DefinitionId 计算；Code A 仅在自身敌人死亡状态已提交后单向调用 observer，且完全忽略 Code B 返回结果。P11 Owner record schema 4→5 增加独立 body section：固定 recipe 的真实 ContainerId／ItemId／child graph、immutable definition/materialization/death receipts、全 `Hidden` visibility 与只读 projection 一次性同记录提交；重复同回执无写入，冲突 target/receipt、Prepared/terminal/失配 P6、非法定义或 stale record 均零写入。P8 close commit 只重置 P11/P9 残余，绝不把 body graph 写入 P5、P6、Code A Loot、世界或结算。旧 M01 corpse/reward 路径未被读、镜像或改写；P11 graph 是尚未挂接 UI/转移的独立 Code B 物品图。2026-08-07 的唯一 Editor 编译以 native exit code `0`、UBT `Succeeded` 完成；真实死亡、重复死亡、恢复、终局、CTA、自动化、回归、截图、Smoke、Game Build、Cook、Package 与最终验收仍全部属于 `0.0.9B.F`。

P12.0.r0 将上述唯一 `CodeB.BodyContainer.BasicCorpse` 接到该 Low Skirmisher 已有 Code A 尸体壳的最小交互适配：adapter 只携带静态 `M01.BodyTarget.LOW.Skirmisher.01.Ordinal.0` 路由、距离、timer 和 Host 生命周期，先只读验证既有 P11 projection，Code B 拒绝／保存／UI 失败不会反馈改变 Code A 死亡、尸体、奖励、地图、Run 或终局。Owner record schema 5→6 为 P11 body section 增加持久化 action id／search item id 与 `BodyMaterialized → Opening → Open`、`Opening → Interrupted`、`Hidden → Searching → Revealed` 状态；`FCodeBBodyContainerInteractionTiming::OpenSeconds = 0.85f`、`ItemSearchSeconds = 0.70f` 是唯一命名 production cadence。P7 Host 以 transient `BodyContainerTarget` 呈现第二栏；Hidden／Searching 格清空 Widget address 的 ItemId／occupied payload，不能泄露或拖拽，且只允许点击发起单件 reveal。唯一位置写链仍是 `NativeOnDrop → P4 → P3 → P2 → P1`，P12 service 以同一 Owner atomic replace 共同提交 P6 玩家图与 P11 body 图，先校验 P1、identity、revisions、visibility、Move/Merge/Swap 与完整 child graph，失败不半提交；放回尸体的玩家物品标为 Revealed，P8 仍只结算 P6 并丢弃尸体残余。P4x、P5、P6 Prepared/rebind、P7 Drop-only、P8 finalizer、P9/P10 semantics、P11 receipt/首次物质化及 Code A 权威均未改动。2026-08-07 最终 `demo_mapEditor Win64 Development` 编译 native exit code `0`、UBT `Succeeded`；真实运行、CTA、自动化、回归、截图、Smoke、Game Build、Cook、Package 与最终验证仍归 `0.0.9B.F`。P12 状态为 `READY_FOR_NEXT_P_FUNCTIONAL_WITH_F_DEBT`。

P13.0.r0 在 P5 Owner sidecar 与 P6 active Run session 内分别持久化**恰好九个** `SlotIndex 1..9 + ItemId / Empty` 的 Code B 快捷栏引用；它不保存数量、定义、位置、容器、child、Widget、Actor 或运行时 GUID。只有当前确实存在、数量大于零、Code B `QuickUsable` 为真且父容器精确等于 P5/P6 `BasicContainerId` 的物品可绑定，同一 ItemId 重绑会原子移除旧引用，解绑只清空目标引用。Profile schema 6→7、Run schema 2→3；旧序列化 definition 仅依据既有 Code A consumable category 迁移为显式 Code B `QuickUsable`，不创建物品、配方或使用效果。P5→P6 bridge 随携行图迁移引用并锁定 P5；P8 Extracted 仅保留确实回到 P5 BaseQuick 的引用，Dead/RecoveredAbandon 丢弃 P6 引用；P7/P10/P12 事务、P8、bridge/rebind 都在同 Owner durable save 前 reconcile 并只清理失效引用，从不自动绑定。P3 生产页显示真实九槽读模型和显式“绑定／清空”动作；不接受拖拽绑定，未加入 1—9 键、使用、消耗或世界效果。P1 图、Code A hotbar、P9/P11 真值及所有既有物品位置写链未变。2026-08-07 的指定 `demo_mapEditor Win64 Development` 编译最终 native exit code `0`、UBT `Succeeded`；未运行游戏、CTA、自动化、回归、截图、Smoke、Game Build、Cook 或 Package。P13 状态为 `READY_FOR_NEXT_P_FUNCTIONAL_WITH_F_DEBT`，功能验证留待 `0.0.9B.F`。
