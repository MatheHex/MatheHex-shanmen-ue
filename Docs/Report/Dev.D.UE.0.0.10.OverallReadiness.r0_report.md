# 《山门》UE 0.0.10 游戏开发总体报告
## 面向 UI 与实际 UE 实装的系统、数据与就绪度评估

- 报告编号：Dev.D.UE.0.0.10.OverallReadiness.r0
- 核验日期：2026-09-14；代码基线：[831c6b1](https://github.com/MatheHex/MatheHex-shanmen-ue/commit/831c6b1efb4e154347f389d424d82fb3f4ba93af)，对应 P27.28。
- 项目目录名仍为 Dev.D.UE.0.0.9B，开发内容已进入 0.0.10；引擎声明为 UE 5.8。
- 本轮仅进行代码、需求、阶段文档和既有日志核验，新增报告及审计记录。未修改产品代码、资产、输入、数值、存档或自动化，也未启动 Editor UI、PIE、Standalone、Cook、Package。
- 文中“已实现”指相应代码/自动化证据存在；“建议”是后续方案，不代表已经实现。“未证实”不等同于断言不存在。

## 阅读前：最新已完成基线补充（2026-09-15 UTC）

本报告的系统矩阵、工程规模及第 7/8 节历史数据仍以开头的 P27.28 基线为准，保留原始审计口径。当前最新已提交并核对远端的实现为 [P27.29 / 58e6964](https://github.com/MatheHex/MatheHex-shanmen-ue/commit/58e696483119137cd4ed3b267b615af812cc7f39)，本次只读复核作以下增补；不将未提交实现或未完成验证计入完成度。

| 项目 | 最新可核验状态 | 对后续准备的影响 |
|---|---|---|
| Run 生命周期 | P27.29 已修复剑法逻辑/表现 owner 身份拒绝时的状态保留和释放顺序 | 不再把 P27.29 当作尚未执行的计划；但不是整个结束流程原子回滚 |
| 自动化 | 重新读取 P27.29 原日志：Shanmen.0_0_10 为 1,416 Success / 0 Fail，明确队列清空；四组旧系统为 116 / 0 | 更新“最新完成测试数”，专项与根组不重复累计；本轮未重跑测试 |
| 构建 | P27.29 Editor / Game run-state 均为 SUCCEEDED，原生退出 0 / 0 | 编译通过仍不等于实际玩家交互或打包验收 |
| 底层冻结 | P27.29 明确保留 World/Timeline/orphan 结束清理待审计项；本次工作区另有 5 个未提交实现/映射文件 | 不将待验证收尾改动算作已交付，不宣称底层已最终冻结 |
| 内存结论 | 重新核对 Repository 全量快照、请求历史、锁内持久化及旧日志启动采样；风险判断不变 | 尚无实际游戏 RAM/VRAM 峰值或帧耗时验收；不能给出已达标结论 |

P27.29 全根日志原字节 SHA-256：`87CB5E8499AFF448ACB38C296AFC54341A200311B21BC8623845A0CAE486B9F5`。阶段细节见 [P27.29 Report](Dev.D.UE.0.0.10.P27.29.r0_report.md) 与 [Development Log](../Log/Dev.D.UE.0.0.10.P27.29.r0_log.md)。本次增补没有修改产品源码、资产、自动化或正在收尾的 5 个文件。

## 1. 总体结论

**当前项目处于“底层框架和大量产品接入口已形成，准备统一 UI 契约与真实 UE 场景验收”的阶段，不是从零开始，也不是已完成可玩版本。**

已有比较可靠的基础包括：确定性身份、统一命中/防御结算、单一物品权威、资源预留与提交、存档故障恢复、Run 生命周期、动作互斥、剑法/御器/暗器/阵法/部分术法的执行与展示接口。部分 HUD、投掷预览、御器外观、神识标记和物品详情已经存在，后续 UI 应复用它们背后的数据与命令接口。

仍不能等同于完成的部分包括：全套正式 UI、动画和特效实际播放、连续玩家输入体验、完整内容目录、数值平衡、长时性能、打包后端到端体验。尤其要避免把“命令成功分派”“回执被确认”“无头 World 测试通过”解释成“玩家已经在正式场景中看见并用好了该功能”。

本报告的准入判断：

| 下一步 | 判断 | 限定 |
|---|---|---|
| UI 信息架构、页面清单、交互状态与数据契约设计 | 可以开始 | 使用现有权威投影，不在 UI 建第二份真值 |
| 架构收尾与冻结清单 | 应优先完成 | P27.29 已完成局部修复，见开头增补；不能当成整体已冻结 |
| 小型灰盒 UE 技术接线准备 | 可以规划 | 先列具体入口、资产缺口和验收用例 |
| 扩充技能、敌人、关卡及最终数值 | 暂不作为当前收尾范围 | 尊重“先确定底层框架，不开始实际游戏性开发”的要求 |
| 宣布正式可玩或性能达标 | 证据不足 | 需真实 F 阶段验证，不能由本报告替代 |

## 2. 系统分层：六个模块与一条清晰边界

### 2.1 逻辑层级

下面是职责图，不是要求立刻重构成新的目录：

```text
玩家输入 / UI / 动画与特效
          │ 请求命令，读取投影
          ▼
产品组合层 demo_map
GameInstance 物品服务 + GameMode Run 根 + Controller/HUD + 各产品 Host
          │ 明确的领域调用
          ├── ShanmenItems：物品、资源事务、持久化
          ├── ShanmenCombatRuntime：动作、能力编排、各战斗体系
          └── ShanmenWorldGameplay：实体身份与 World 几何适配
                         │ 候选，不直接决定伤害
                         ▼
             ShanmenCombatCore：命中契约、纯结算、去重
                         │
                         ▼
                 ShanmenCore：基础身份与内容戳
```

### 2.2 实际模块职责

| 模块 | 已有职责/依赖事实 | 不应承担 |
|---|---|---|
| ShanmenCore | 基础类型、确定性 ID、内容版本身份；依赖 Core/CoreUObject | 玩法规则、库存和 World |
| ShanmenCombatCore | Action 快照、HitCandidate、DamagePacket、生命值上下文、有序防御层、Impact Ledger；有 GameplayTags，无 Engine/GAS 依赖 | Actor 查找、资产、直接扣库存、动画时序 |
| ShanmenItems | 定义/实例/容器/预留/事务回执；Repository、持久化及服务；无 Engine 依赖 | 技能直接计算命中，UI 私下更改数量 |
| ShanmenWorldGameplay | Entity Registry 与命中采集适配，有 Engine 依赖 | 根据几何事件自行形成第二套伤害权威 |
| ShanmenCombatRuntime | 动作与各能力运行契约，依赖 Engine、GameplayAbilities、GameplayTasks、CombatCore、WorldGameplay | 让 GAS 覆盖纯结算或物品真值 |
| demo_map | 现有游戏产品层、旧版本兼容、Run 组合根、UI/输入、Actor/World 执行、各系统宿主 | 继续把所有新能力堆入 GameMode；绕过下层事务 |

模块拆分方向正确，但尚不是“六个均匀、独立、低耦合的系统”。绝大部分生产代码仍在 demo_map；许多新契约也通过它完成产品组合。**下一步需要稳定模块间接口和所有权，不应仅为目录好看而大规模搬文件。**

### 2.3 工程规模

统计仅含 Git 跟踪的六个模块内 .h/.cpp/.cs，包含空行和注释；测试按文件名 Test/Tests.cpp 或 .h 分类，不是运行时包大小或覆盖率。

| 模块 | 文件数 | 非测试行数 | 测试文件数 | 测试行数 |
|---|---:|---:|---:|---:|
| demo_map | 952 | 270,732 | 214 | 143,498 |
| ShanmenCombatCore | 9 | 877 | 1 | 317 |
| ShanmenCombatRuntime | 90 | 19,607 | 27 | 12,059 |
| ShanmenCore | 6 | 142 | 0 | 0 |
| ShanmenItems | 16 | 8,753 | 3 | 3,606 |
| ShanmenWorldGameplay | 11 | 863 | 2 | 527 |
| 合计 | 1,084 | 300,974 | 247 | 160,007 |

测试行约占上述总行数的 34.7%；demo_map 约占非测试行数的 90.0%。代码基线有 394 份 0.0.10 阶段 Report。数量说明工程和验证投入，不能换算成游戏完成百分比。

维护热点包括 V3ProgressionManager.cpp（19,516 行）、CodeBOutOfRaidProfile.cpp（15,633 行）、CodeBP3UI.cpp（11,691 行）、GameMode.cpp（8,742 行）。UI 新增应从明确的读写端口接入，避免复制这些大文件中的业务逻辑。

## 3. 信息分区与数据权威

### 3.1 五类数据分区

| 分区 | 数据举例 | 所有者/生命周期 | UI 的角色 |
|---|---|---|---|
| 内容定义区 | 物品定义、动作定义、阵图、标签、版本戳 | 内容目录与配置；跨 Run 使用 | 显示名称、图标、说明，不能当玩家当前状态 |
| 持久真值区 | 实例数量、耐久、次数、容器位置、预留、处理过的请求 | ShanmenItems Repository/AuthorityService；GameInstance 级产品持有 | 读取投影，通过命令申请变更 |
| 成长/知识区 | 已学阵图、熟练度、角色成长 | 现有成长/存档权威；知识与熟练度 Adapter 单向读取 | 显示解锁和不足原因，不能直接赋予能力 |
| 局内真值区 | Run 身份、生命/状态、动作占用、灵力、部署与活动能力 | Coordinator 与各领域 Host/Session/Lifecycle | 显示当前值，不跨 Run 复用旧对象 |
| 展示/诊断区 | 血条插值、提示、图标、路径预览、动画音效命令、诊断回执 | UI/表现消费者；短期缓存，诊断可独立存档 | 可重建、可丢弃视觉缓存，但不能误删事务恢复证据 |

“同一资源只有一个权威”应按领域理解：物品数量归 ShanmenItems，生命/状态归相应战斗宿主，动作占用归仲裁器；不是把所有真值塞进一个超级对象。

Code A 和 Code B 仍存在于源码和历史场景中。0.0.10 物品 ADR 已明确它们是旧来源/兼容边界，而非两套在线物品权威。新 UI 不得同时读取 Code A 数量、写 Code B 仓库、再调用新 Repository 扣款。当前实体实例 ID 必须贯穿仓库、携带、战斗消耗与撤离。

### 3.2 区间切换顺序

```text
局外仓库/整备
  → 校验实例与携带方案
  → Reserve：冻结资格，不代表已消耗
  → Commit/StartPreparedRun：形成持久证据
  → Run 根激活，World/运行宿主就绪
  → 局内命令与消耗
  → 撤离/死亡结算
  → 持久结果确认
  → 按所有者顺序清理 Run
  → 重建局外 UI 投影
```

进入关卡动画、点击“出发”或生成 Actor，都不能独自证明整条链已成功。失败后只重试未完成的同一请求；不以新 ID 重做已经成功的扣除。

P27.28 阵法散布发布已接入 GameMode → FormationRunLifecycle 的唯一入口。Run/Handoff/World 不匹配时拒绝；结束失败保留恢复所需的 Coordinator、Timeline 与检查点。这是重要的产品组合证据，但该阶段明确没有加入物理输入和正式地图接线。

### 3.3 时间、事件与刷新

- 现有 CombatRunFixedTimeline 消费 GameMode 的 DeltaSeconds，暴露 Run 绑定的整 30 Hz tick；**它不是整个游戏 30 FPS，也不证明所有系统锁步或联网确定性**。
- 权威时间负责动作窗口、护盾到期等判断；UI 可平滑插值，不得反向改变权威倒计时。
- 状态投影用于“现在是什么”；带身份的事件用于“发生过什么”。用事件重建动画，用状态修复晚加入或刷新后的 UI。
- 每个消费者独立记录已处理位置；关闭一个面板不应消费掉另一个面板需要的事件。
- 领域回执的持久确认与表现消费者的接收确认分开。展示 ACK 不是“动画/音效已真实播放”。
- 线程安全锁不是“异步”承诺；UObject/World 操作不能因引入后台存盘而被任意转移。

## 4. 系统完成度矩阵

完成度使用证据等级，不给缺少分母的 80%/90%：

- **D0：方向/需求**，未有完整可核验运行闭环。
- **D1：契约/纯逻辑**，结构与算法可验证。
- **D2：产品接入及自动化**，有宿主、适配、生命周期或无头 World 证据。
- **D3：真实 UE 交互验收**，需实际输入、可见表现、跨场景与失败恢复。
- **D4：版本交付验收**，需打包、目标机器性能、内容和体验回归。

下表评级按本次能引用的证据给出；D2 不自动包含 D3，局部表现代码也不代表全系统 D3。

| 系统 | 已有能力与证据 | 当前判断 | UI/实装前仍需确认 |
|---|---|---|---|
| 基础身份、内容戳 | 确定性 Activation/Impact ID、身份复算、不可变快照 | D1，已广泛被 D2 使用 | UI 使用稳定 ID；内容版本切换规则 |
| 命中与统一防御 | 标签筛选、有序层、生命值上下文、致死拦截、伤害守恒；P0.1 | D1/D2 | 每种正式伤害/装备配置；真实命中表现 |
| 物品/容器/事务 | Reserve/Commit/Cancel、幂等、次数/耐久、持久化恢复；P1 系列 | D2，框架重点已落地 | 大档性能、最终容量策略、玩家恢复界面 |
| 旧存档迁移 | 严格来源一致性、旧 schema 支持、导入后单一物品权威 | D2，有测试与 ADR | 发布用真实旧档矩阵，不把合成测试当全部存档 |
| Run/整备/撤离/死亡 | 持久启动、运行绑定、结算/清理、失败检查点；P1.14、P27.28 | D2 | 完整地图旅行、重启、UI状态与落盘一致性 |
| World 几何与实体 | Registry、Sweep/Overlap/Projectile 候选；P2 | D2 | 实际碰撞配置、Actor 类和场景覆盖 |
| 剑法/太极节奏 | 动作阶段、节奏/继承、打断/事件链；P3、P12 | D2；资产播放不由此证明 | Montage/Notify、操作容错、摄像机与手感 |
| 剑气 | 可用性投影、带版本命令、失效拒绝、受阻提示；P18、P24 | D2 + 局部 HUD | 输入到命中全链、视觉资源与正式消耗 |
| 御器/飞剑 | 实体物品关联、运行生命周期、运动/展示与可用性；P6、P21 | D2 + 局部外观 | 贴身威慑/防御的具体规则，实际表现验收 |
| 暗器 | 数量事务、快捷栏、直线/弧线、预览、发射起点；P7、P20、P22 | D2 + 较多预览接口 | 不同熟练度的正式输入；高级自动选路不能因有枚举便算完成 |
| 阵法 | 图纸/材料/部署/影响、熟练度权限、散布交接与 World 发布；P8、P27 | D2；最新底层入口闭合 | 材料选择、范围/时机、真实交互、阵图资产 |
| 灵力与护盾 | 共享产品灵力读数、护盾会话/期限/形态与显示；P9、P25 | D2 + 局部表现 | 多能力同时消费、体验、目标平台预算 |
| 闪避/格挡/动作仲裁 | 重绑输入、互斥/抢占、精确所有者、过期状态重查；P10、P11 | D2 | 延迟输入、连按、焦点丢失、动画可读性 |
| 状态/伤势/治疗 | 条件意图→副作用→证明→物品提交，未知结果失败关闭；P16 | D2 | 状态完整目录、玩家可理解的治疗/失败提示 |
| 护心镜等被动灵器 | 致死触发、实际次数持久消耗、死亡/撤离一致性；P17 | D2 | 不可据此宣称所有灵器或精神防护均已做完 |
| 神识/感知 | 目标感知、单个待处理命令、重试/取消、HUD边缘/避让；P19、P23 | D2 + 局部 HUD | 正式目标类别、遮挡与信息密度、实际操作 |
| 装备详情/防具 | 物品目录投影、法袍耐久/抗性详情；P5、P26 | D2 + 局部详情 | 全装备目录、元素/毒/异常属性的正式配置 |
| HUD/输入提示/展示投递 | 可用性、预览、键位、显示层消费与恢复；P13、P14、P20 | D2；不是统一成品 UI | 页面视觉规范、焦点/手柄/分辨率/无障碍 |
| 敌人/AI/战斗地图 | 旧敌人技能、远程兼容及 M01 统一伤害接入；P4 | 历史基础 + D2 接入 | 0.0.10 正式敌人设计、难度和真实遭遇验收 |
| 成长/知识/熟练度 | 旧成长系统；新知识/熟练度只读 Adapter 与分级权限 | D1/D2 接缝 | 经验来源、升级保存、完整技能树不是由 Adapter 自动提供 |
| 生产/炼丹/宗门/交易经济 | 人工规划明确方向，存在部分旧资源/成长基础 | D0 或历史基础，未证实 0.0.10 完整闭环 | 不应把占位货币/旧 UI 当正式经济系统 |
| 环境术法/地形改造等 | 需求方向明确，未见本次足以证明完整实现的交付 | D0/未证实 | 地形权限、生命周期、成本与资产设计 |
| 联机、发布、性能 | 不能由 USTRUCT、确定性 ID 或编译成功推导 | 未证实 D3/D4 | 网络权威/复制若需要须单独设计；Cook/Package/硬件实测 |

## 5. 游戏系统与“分级”的产品含义

### 5.1 五大体系不是五种伤害颜色

| 体系 | 玩家核心动作 | 必要准备 | UI 优先呈现 |
|---|---|---|---|
| 剑法 | 近战、时机衔接、格挡/闪避后继承、剑气 | 武器与动作配置 | 动作可用性、节奏窗口、强化来源、受阻原因 |
| 御器 | 携带真实器物，环绕/发射/控制 | 装备实例、灵力、部署资格 | 可控器物列表、活动/回收状态、资源 |
| 暗器 | 直投、弧投、瞄准与投掷 | 消耗物实例、数量、熟练度 | 数量、当前模式、路径、目标/落点有效性 |
| 阵法 | 空间布置、材料投入、启动与维持 | 阵图知识、材料、灵力、熟练度 | 阵图/锚点、缺料、范围、进度、撤销边界 |
| 术法 | 感知、防御、机动、控制、环境影响 | 对应能力与资源 | 可用状态、持续/剩余量、作用对象 |

最终伤害公式、境界倍率、经验曲线、货币统一口径和数值平衡尚不应在 UI 层提前固化。

### 5.2 当前可以引用的操作分级

- 阵法已存在 Beginner / Intermediate / Master 策略及 ProximityFill / RemoteThrow / ScatterFormation 三种操作。高等级保留下级能力；范围、时间、材料和 World 执行属于外层，不包含在等级枚举中。
- 暗器已有 Beginner / Intermediate / Master 请求分级；弧线规划对 Beginner 不开放。**Master 枚举存在并不证明“自动智能选路”已经完整落地。**
- 剑法、御器、术法不宜为了页面对齐而强行套用同一三级经验表。界面结构可以统一为“当前能力—下级/上级能力—解锁条件”，具体成长规则由策划与成长权威提供。
- 物品分类建议保持原需求中的原料/建造、战斗消耗、装备/灵器、知识/图谱四类；不要把灵矿、灵石、灵液提前显示成同一种已确定货币。

## 6. 面向 UI 的信息交互分区

### 6.1 页面分区及读写边界

| UI 分区 | 核心信息 | 允许命令 | 必须禁止 |
|---|---|---|---|
| 局外总览/仓库 | 资产、物品位置、状态、可用装备、知识 | 通过权威端口移动/选择/整备 | UI 拖拽成功就先改真实数量 |
| 出发整备 | 携带、部署、预留、缺失条件、Run 准备状态 | 校验、准备、取消、出发 | World 出现即显示持久提交成功 |
| 战斗 HUD | 生命、灵力、护盾、状态、快捷栏、动作可用性 | 发起一次有身份的能力请求 | 自行扣灵力、自行结算伤害 |
| 上下文交互/瞄准 | 落点、投掷轨迹、阵图锚点、目标、受阻原因 | Issue / Cancel；明确的 Retry | 将每帧预览当真实发射或消耗 |
| 物品/能力详情 | 属性来源、次数/耐久、模式与条件 | 查看、合法装备/使用请求 | 用显示文案决定实际抗性/能力 |
| 结算与恢复 | 战利品、损耗、已保存/待恢复状态 | 查询结果、重试同一未完成请求 | 为使页面好看而丢弃错误或重扣 |
| 开发诊断（独立） | Run/Request/Revision、回执、耗时/队列长度 | 受控诊断 | 将技术 JSON、哈希、协议信封塞进玩家界面 |

空间布局建议：中央保留瞄准/路径与危险可读性；生命/灵力/状态形成稳定区域；底部动作与快捷栏；边缘处理神识目标；临时提示短时出现。具体像素、画风、镜头、手柄与最终比例在原型阶段确定，不把现有 Canvas 坐标视为最终设计。

### 6.2 统一的 UI 状态契约（建议）

各页面至少可表达：

1. Loading / Unbound：数据未绑定，不能用零值假装资源耗尽。
2. Ready：展示带 Run/Owner/Revision 或 ProjectionId 的快照。
3. Pending：请求在处理，按钮防止重复提交，但不伪造结果。
4. Applied / Replayed：结果已按所属领域的成功规则证实，再更新投影。
5. Rejected / Stale：显示可理解原因；过期投影先刷新，不偷偷重新消费。
6. RecoveryRequired：结果不确定或持久权威需恢复；保留上下文，不退回“可再次购买/发射”。

典型事务：

```text
玩家点击
→ UI提交“动作+目标+当前投影身份”
→ Router检查新鲜度/可用性
→ 领域宿主执行，必要时调用物品事务
→ 权威结果与新投影
→ UI显示状态；表现消费者接收一次视觉/音效命令
```

剑气已有 ExpectedProjectionId 检查。Router 的 Dispatched 只说明调用发生，需进一步检查内层回执；新页面应继承这种区分。

### 6.3 刷新和缓存策略（建议，不是现状已全部满足）

- 数量、耐久、成长、权限：按 revision/变化事件重建小投影；不在每次 DrawHUD 中复制整份历史物品快照。
- 瞄准、屏幕坐标、血条平滑：按可见需求刷新；只更新必要字段。
- 提示与命令回执：事件驱动且有去重；UI 关闭/重开能从状态重新同步。
- 列表虚拟化/复用只影响显示控件，不改变物品实例和容器权威。
- 表现对象使用清晰所有者、失效检测和释放规则；缓存不得让旧 Run 的 Actor、Session、数据一直存活。
- 正常玩家只看原因与可执行选项，技术身份和原始错误进入诊断面板。

## 7. 数据计算、内存与性能风险

### 7.1 现在真正量到什么

既有 P27.28 完整根组日志在 **2026-09-14 03:08:30.367 UTC** 的启动采样显示：

| 指标 | 原日志数值 | 正确解释 |
|---|---:|---|
| 进程 Physical Memory | 628.29 MB used / peak | 此采样时的无头 UE 进程；不是完整运行峰值 |
| 进程 Virtual Memory | 648.35 MB used / peak | 日志所报进程虚拟内存指标；不能当显存 |
| 系统 Physical Memory | 22,012.75 MB used / 32,502.40 MB total | 整台机器，包括其他程序 |
| 系统 Virtual Memory | 50,906.70 MB used / 106,230.40 MB total | 整机指标，不能归到本项目 |

测试主体 03:08:43.986 UTC 才开始；运行参数包含 NullRHI、NoSound。因此该样本**不能证明实际地图、纹理、Niagara、音频、UI 或 VRAM 占用**。本次没有运行性能采样，也没有得到真实游戏平均/峰值 RAM、VRAM、Game Thread 或 GPU 帧耗时。

源码行数、20 MB 的模块 DLL、360 MB 的 Game exe、存档字节数，都不能直接等同于运行内存。

### 7.2 已确认的高优先级风险：物品历史与全量状态处理

源码入口：[快照导出与排序](../../Source/ShanmenItems/Private/ShanmenItemRepository.cpp#L1225)、[历史记录写入](../../Source/ShanmenItems/Private/ShanmenItemRepository.cpp#L1674)、[同步事务服务](../../Source/ShanmenItems/Private/ShanmenItemAuthorityService.cpp#L402)、[持久化与读回](../../Source/ShanmenItems/Private/ShanmenItemPersistence.cpp#L628)。本节按开头所列提交评估。

对当前代码的静态核验发现：

1. Repository 有 ProcessedRequests 历史映射，RecordProcessed 按请求加入。审计的 ShanmenItems 路径中未发现运行时裁剪/压缩或最大历史条数策略。ADR 也把历史清理留待以后；不能宣称“所有 Journal 都无限”，因为其他局部 Journal 有明确上限。
2. Repository.cpp 有 **26 个**“FState Candidate = State”拷贝代码位置，分布于不同命令/拒绝路径。**这是静态位置数，不是每个命令执行 26 次拷贝。**
3. CaptureSnapshot 会把五类映射导出数组，包括全部 ProcessedRequests，然后排序；不是廉价的引用读取。
4. ExecuteCommandLocked 保留 BeforeDocument、命令前快照与命令后快照，并在锁内执行持久化。Repository 与 Document 本身也持有各自的数据表示。
5. 持久化会校验/规范化、创建 JSON/字节、写临时文件、flush、读回校验、备份、替换和再次核对；大档时 CPU、临时内存与磁盘延迟会叠加。
6. 数据完全不变时有“不写盘”分支；但仍有规范化和磁盘读回校验，不能认为幂等重放完全无成本。
7. ValidateState 的部分路径按 intent 遍历历史事件，FinalizeRun 也有嵌套扫描。总体成本不是所有命令都 O(1)；某些检查随两组历史数量乘积增长。

**风险判断：** 这首先是“随使用量增长的状态和事务成本”，尚无证据把它诊断为实际内存泄漏或已经卡顿。若这些同步命令直接在 Game Thread 上执行，大档写盘可能成为帧卡顿来源；需要追踪调用线程与耗时，不能凭服务带 Mutex 就认为已异步。

解决方向应先测再改：记录历史条数/字节和事务耗时；设计以完成 Run 为边界的归档/压缩方案；保留活动 Run、未决意图与重试必需证据。**不能简单 Remove 旧 ID、关闭校验或延迟成功确认来换性能，否则会破坏不重复消费和崩溃恢复。**

### 7.3 其他内存/计算热点

| 类别 | 代码事实或需测风险 | 后续处理方向 |
|---|---|---|
| 战斗 Impact Ledger | 每 Run 保存已接受的 GUID；有 Reset；大规模持续命中仍会增长 | 测量每 Run 峰值和结束释放；不按 UI 隐藏状态清理 |
| 防御纯结算 | 每次校验/排序防御层，构建索引数组和逐层回执；排序同 Order 时使用 GUID 字符串 | 先测命中频率×防御层数；必要时复用不可变排序结果，保持确定性 |
| Preview/Recovery | 部分结构有完整快照、身份和回执副本 | 核查生命周期和嵌套复制；不能把“值类型”理解为没有堆分配 |
| 有界 Journal | 暗器 ArcPreview OwnerSurfaceHandoffRecoveryJournal 明确最大 16 条，满时拒绝 | 内存有界仍需定义满载后的合法恢复/轮换，不能无限卡在拒绝 |
| 元数据与完整状态 | 剑法 Retry Journal Manifest 编码固定 48 bytes | 48 bytes 只是清单，不含下层完整 checkpoint，不代表恢复内存很小或已可独立落盘 |
| UI 投影 | 全量物品快照导出较重；高频读需要审查 | 小型只读投影、revision缓存、列表复用；本次未断言所有 UI 都在每帧全量复制 |
| World 对象 | 飞剑、弹体、阵法、标记、组件、特效增加对象与 Tick/GC 压力 | 统计活动对象、释放滞留、生成/销毁峰值；对象池也必须明确身份重置 |
| 资产与显存 | 无头日志不含正式渲染预算 | 单独测纹理、网格、材质、Niagara、音频和 UI 图集 |
| 同时构建/运行 | 编译器、编辑器、浏览器等共用整机内存 | 区分工具占用与游戏进程；不据整机占用购买硬件 |

### 7.4 估算模型：说明风险，不冒充实测

建议拆开记录：

```text
进程内存
= 引擎/模块基础
+ 资产与资源
+ 活动 World/Actor/组件
+ 领域真值和历史
+ UI/表现缓存
+ 同时存在的快照/序列化/读回缓冲
+ 分配器余量
```

若假设一条历史记录的平均总占用为 1 KiB，10,000 条约 9.77 MiB，100,000 条约 97.66 MiB；若某时刻有四份等量历史副本，对应约 39.06 / 390.63 MiB，尚未计 JSON、映射余量和资产。

这里的 1 KiB、四份都是**容量情景假设**，不是 sizeof 实测，也不是声明每条命令固定四份。真实值必须包含字符串、数组容量、容器桶与嵌套数据；只量 USTRUCT 的 sizeof 会低估。

### 7.5 后续性能采样与门槛建议

先不承诺最低配置。以同一台目标机、相同 build/地图/分辨率建立可重复基线：

| 场景 | 需要记录 | 验证目的 |
|---|---|---|
| 空场景 → 完整场景 | RAM、VRAM、对象数、加载峰值 | 分离引擎、资产和功能成本 |
| 物品 1k/10k，历史 1k/10k/100k 梯度 | 命令 P50/P95/P99、快照/保存时长、峰值临时字节 | 找增长曲线与同步卡顿 |
| 连续命中、御器、暗器、阵法同时活动 | Game/Render/GPU 耗时、分配速率、对象峰值 | 检验真实规模，而非仅单个对象 |
| 连续 20 次开始/结束 Run | GC 后存活对象、领域记录、RAM趋势 | 区分缓存高水位和不能释放的泄漏 |
| 所有 UI 面板反复开关、改变分辨率/输入设备 | UI时间、布局、过期引用、焦点和订阅数 | 确认展示层不会积累状态 |
| 中断/重开/存盘故障 | 文件恢复、次数/数量、重复副作用 | 优化不能破坏数据可靠性 |

建议以 60 FPS 的 16.67 ms 作为首轮讨论目标，不是当前已达到的指标；UI 更新初始预算可设 P95 ≤ 1 ms、P99 ≤ 2 ms，目标机确认后再冻结。交易若不能进入对应帧预算，应明确 Pending 体验与受控执行策略，而非先扣再补。

长期运行验收应看 warm-up/GC 后趋势，不要求内存瞬间回到精确初始字节。需要分别记录 RAM、commit、VRAM 和各线程耗时，不能拿一个“总内存”指标代替。

## 8. 验证证据与完成度限制

本次重新读取原始日志并核对 SHA-256 与结果计数；**未重跑以下测试**：

| P27.28 既有证据 | 核验结果 | 能证明什么 |
|---|---|---|
| Shanmen.0_0_10 全根 | 1,415 Success / 0 Fail；队列清空 | 当前根组自动化结果 |
| FormationRunLifecycle | 13 / 0 | 生命周期专项 |
| GameModeScatterPublication | 3 / 0 | 最新组合入口专项 |
| 旧系统四组 | 4 + 44 + 22 + 46 = 116 / 0 | Attributes、EnemySkillFramework、V2RangedCompatibility、ItemUseAndArmor |
| 覆盖映射 | Changed=6 / Rules=7 / Required=83 / Logs=7，PASS | P27.28 改动与必跑证据匹配 |
| 映射自检 | 504/504 PASS | 检查器自身用例 |
| 最终 Editor / Game 构建 | Succeeded，原生退出 0 / 0 | 编译与链接，不是产品启动 |
| 第一次专项失败 | 2 Success / 1 Fail，原日志保留 | 故意触发的 Error 未登记为预期；后续测试夹具修正 |

专项组与完整根组有重叠，不能相加称作新的独立测试总数。零失败也不等于覆盖率 100%。

完整根日志 SHA-256：
`75DEE689E683210ACE250292D8086DFC24D8663F6E148BB3513AE4421E42D930`。
其余日志哈希和静态统计见[证据索引](../Log/Dev.D.UE.0.0.10.OverallReadiness.r0_evidence.json)。

当前全根运行约 1 小时 25 分 25 秒，已不同于早期“106 个纯值测试约 1 秒”的规模和用例组成。不能继续宣称所有回归都近乎零成本；也不能因此裁掉受改动文件影响的旧测试。建议日常按文件映射运行，冻结/发布时补完整范围，并区分快速纯逻辑与较慢 World/持久化组。

本次未重跑完整旧 demo_map 根、真实地图、玩家输入、UI、打包或性能。历史 P15.10 的旧根成功也不能当成本次重跑。

## 9. 关键债务与优先级

| 优先级 | 事项 | 判断及准备动作 |
|---|---|---|
| A：UI 前必定清楚 | 每页数据来源、写入口、Run/Owner/Revision | 先形成契约矩阵；禁止新 UI 双写旧/新库存 |
| A：大量接线前 | 底层冻结与唯一产品入口索引 | P27.29 已完成局部修复，World/Timeline/orphan 等相邻分支仍需完整证明；见开头增补 |
| A：真实性能前 | 物品历史增长、全量拷贝与同步持久化 | 高可信静态风险，但尚无运行数值；先测量，再决定优化 |
| A：玩家可理解性 | Pending、过期、失败、恢复与已持久成功 | 错误不可只显示“按钮没反应”，也不能伪成功 |
| B：维护一致性 | 早期 ADR/阶段结论过时 | ItemAuthority ADR 仍写 schema 1..5 缺陷，当前源码已是 1..current-1；本报告指出，不复述为未修产品故障 |
| B：表现验收 | ACK 与实际资产播放 | 对动画、音效、特效、视觉对象分别验收 |
| B：长期运行 | Journal 满载、缓存/事件订阅/Actor 释放 | 对有界和无界集合分别定义生命周期 |
| B：规模和可维护性 | 巨型 demo_map、长回执/恢复链 | 用索引和端口封装降低接线成本；避免无需求扩充恢复包装层 |
| B：成长/内容 | 熟练度权限与真实升级流程的差距 | 不让 UI 假装经验曲线/高级能力已完成 |
| C：后续版本范围 | 宗门生产、交易、环境术法、联机 | 当前未证实完整实现；与首轮 UI/灰盒切片分开 |

过去“单标量防御、没有致死上下文、ImpactId 可伪造、标签惰性”的早期问题，当前核心源码已分别出现有序层、Vitality、ID 重算和标签过滤，不应再按最初 P0 结构评估。反过来，这些修复也不能替代所有装备和所有伤害类型的正式内容配置。

## 10. 为下一阶段准备的交付清单

### 10.1 首先收尾：冻结的是边界，不是最终数值

建议冻结：身份派生、领域唯一权威、资源提交点、不可变输入/结果、Run 生灭、拒绝和恢复规则、UI命令/投影边界、改变文件对应回归组。

暂不冻结：最终武器数值、经验曲线、敌人难度、技能资产、UI风格与像素、目标硬件预算。未知项应明确列出，不用默认值装作完成。

本报告是准备评估，**不替代一次执行过的最终冻结审计/全套验收**。

### 10.2 UI 开发准备包（建议下一轮文档/契约工作）

1. 页面与数据源清单：仓库、整备、战斗 HUD、物品详情、瞄准/阵法、结算、恢复。
2. 每个控件的字段表：稳定 ID、值、单位、来源、刷新条件、空值/过期处理。
3. 每个按钮的命令表：前置条件、输入、成功标准、拒绝原因、取消/重试规则。
4. 明确共享提示文案与错误到玩家语言的映射；技术细节留在诊断区。
5. UI 资产清单：字体、图标、状态符号、面板、路径/目标标记；依靠文字和形状，不只依靠颜色。
6. 键鼠/手柄焦点、重绑、多分辨率、超宽屏、长文本与缩放原型检查。
7. 契约测试：旧投影拒绝、重复点击不重复消费、重开页面恢复、跨 Run 引用失效、取消不吞掉已提交结果。

### 10.3 UE 实装准备包（本轮不执行）

选定一个技术灰盒切片：整备 → 进入 M01 或明确的测试地图 → 一种攻击/防御/消耗 → 结算 → 重开验证。先证明数据、输入、Actor、UI一致，不加新玩法设计。

需要逐项准备：GameMode/Controller/HUD 类、Subsystem 初始化、输入映射、Actor/组件类、碰撞通道与目标注册、骨骼/手部 socket、Montage/Notify、投掷/御器网格、阵法材质、Niagara、音效、保存目录隔离、日志和诊断开关。资源缺失必须有明确占位提示，不能以“收到表现命令”充当实际播放成功。

仓库已跟踪六张地图，含 M01 Expedition、V2 CombatDemo、V3 ProgressionDemo、TopDown、Strategy、TwinStick。**地图文件存在不证明其在本版本已完成接线与验收**；不应同时以多个旧 Demo 作新的真值来源。

### 10.4 分步准入

| 顺序 | 输出 | 通过条件 |
|---|---|---|
| 1. 框架收尾 | 入口/所有权索引、待解决项、冻结说明 | 每条关键能力能找到唯一产品入口，无未知双写 |
| 2. UI 契约与灰盒设计 | 页面图、投影/命令表、状态模型 | 信息来源一致、失败可解释、不会重复消费 |
| 3. 最小 UE 接线验收 | 真实输入与可见表现的短闭环证据 | World、UI、资源、持久结果一致 |
| 4. 性能和稳定性 | 内存/帧耗时/长时/故障恢复数据 | 有目标机、场景、阈值和原始记录 |
| 5. 再进入正式内容开发 | 动画/技能/敌人/关卡/数值迭代 | 不用内容堆叠掩盖基础未闭合 |
| 6. 版本交付 | Cook/Package、包体/目标硬件/存档回归 | 单独形成 D4 证据 |

本轮只完成总体报告与证据整理，未自动开始以上任一实现阶段。

## 11. 核心来源与检索入口

阶段证据按当前阶段的能力理解，早期“尚未实现”可能已被后续阶段覆盖：

- [P0.1 核心契约修正](Dev.D.UE.0.0.10.P0.1.r0_report.md)
- [P1.14 Run/物品权威闭环](Dev.D.UE.0.0.10.P1.14.r0_report.md)
- [P12.33 剑法恢复清单边界](Dev.D.UE.0.0.10.P12.33.r0_report.md)
- [P13.2 表现命令/确认](Dev.D.UE.0.0.10.P13.2.r0_report.md)
- [P16.9 治疗事务](Dev.D.UE.0.0.10.P16.9.r0_report.md)
- [P17.2 护心镜](Dev.D.UE.0.0.10.P17.2.r0_report.md)
- [P18.10 剑气投影新鲜度](Dev.D.UE.0.0.10.P18.10.r0_report.md)
- [P20.66 提示布局](Dev.D.UE.0.0.10.P20.66.r0_report.md)
- [P21.15 御器表现](Dev.D.UE.0.0.10.P21.15.r0_report.md)
- [P22.12 投掷起点](Dev.D.UE.0.0.10.P22.12.r0_report.md)
- [P23.4 神识 HUD](Dev.D.UE.0.0.10.P23.4.r0_report.md)
- [P24.6 剑气阻塞提示](Dev.D.UE.0.0.10.P24.6.r0_report.md)
- [P25.9 护盾表现](Dev.D.UE.0.0.10.P25.9.r0_report.md)
- [P26.5 物品详情](Dev.D.UE.0.0.10.P26.5.r0_report.md)
- [P27.28 最新 Run 组合与回归](Dev.D.UE.0.0.10.P27.28.r0_report.md)
- [物品权威 ADR](../Architecture/Dev.D.UE.0.0.10_ItemAuthority_ADR.md)、[事务 ADR](../Architecture/Dev.D.UE.0.0.10_ItemTransactions_ADR.md)、[迁移 ADR](../Architecture/Dev.D.UE.0.0.10_ItemMigration_ADR.md)
- [本轮审计 Log](../Log/Dev.D.UE.0.0.10.OverallReadiness.r0_log.md)、[机器可读证据索引](../Log/Dev.D.UE.0.0.10.OverallReadiness.r0_evidence.json)

**最后判断：可以开始“让玩家看懂并正确操作现有系统”的准备工作；不要继续把更多协议层、更多测试数量或更多报告，视为进入实际游戏阶段的替代物。先把现有能力的唯一入口、UI契约、真实表现和容量边界讲清、测清，再做内容。**
