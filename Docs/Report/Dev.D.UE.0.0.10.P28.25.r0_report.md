# Dev.D.UE.0.0.10.P28.25.r0 Report

## 1. 状态与范围

`P28_25_PASS`。基线P28.24 / `c4f2d59846980ccd6b7115ae7cb906670aa44c64`；2026-09-21 UTC。本阶段闭合空间包部分生成失败时的原World投影归属，完整回归、双目标构建和改动覆盖检查通过。不增加玩法、持久schema或恢复平台；这是有界阶段完成，不是整体冻结或F验收。

## 2. 已复现的结构缺口

既有Manager确认丢弃路由调用Runtime.DiscardSpatialItemBundle。它先改变Runtime归属，再生成各个World对象；旧回滚先移除已生成对象的绑定，忽略Destroy拒绝后恢复物品快照，导致仍存活的投影失去原owner。

既有PreparedWorldPickupIdentity追加瞬态夹具：实际生成前两个对象，在第二个对象的引擎生成回调中设置已有World teardown拒绝标记，使第三次SpawnActor实际返回空；第二个对象同时使用引擎已有role条件拒绝Destroy。首次Red为0成功/1失败、精确队列1、原生0、崩溃指标0，最终Expected失败明确指向“部分生成回滚保留原拒绝投影”。没有新增生产故障端口。夹具只证明生成拒绝与回滚组合，不冒充完整World shutdown或所有失败原因。

## 3. 修复与验证范围

先恢复原逻辑快照及修订，再逐项释放本次原投影；拒绝项复用P28.24的原BundleId待释放回执和绑定，后续沿普通拾取续清理，不重做物品转移。保留失败生成的返回码，同时区分“逻辑已恢复”和“World仍待释放”。只清本次空间包记录，不再用全量清空影响其他包。

测试保留原P28.24回收/终局组合，新增失败生成、非零库存/携入/修订和durable Run保持、重复释放拒绝、竞争丢弃门、恢复后原对象恰好一次释放，以及不拒绝释放的健康回滚变体。Red在原owner保持断言后提前清理退出，因此后续变体没有独立Red，不夸大首次失败覆盖。

首次修复链exec73453在Editor成功后停于拾取专项0成功/1失败（原生0、队列1），未运行后续专项、Game或完整根。源码核对发现：已接受Destroy的对象会使弱绑定失效，此时再用原对象指针做ExpectedActor检查会提前返回，留下陈旧绑定。修正该本次已拥有实例的清理调用，与既有回收路径一致；未删除或放宽失败断言，保留首次修复失败原件。

02:59:53.9386060UTC重新锁定1617产品/验证输入和103原用户文件，Recovery1链重新执行Editor→专项1/7/5→Game→旧根→新根；原Final锁及失败证据另存保留。新根于04:16:08.8687294UTC正常结束，12:30UTC根据原始run-state及完整日志核验，不重跑或拼接历史结果。

| 本阶段验证 | 实际结果 |
|---|---|
| Editor / Game Development构建 | 原生0 / 0 |
| 拾取 / World生命周期 / 产品流程专项 | 1/0、7/0、5/0，精确队列及正常退出 |
| 完整旧根 / 完整0.0.10新根 | 1330/0、1430/0；共2760个独立成功用例 |
| 六路径改动驱动回归 | 1条规则、3个必跑组、2份本阶段完整日志，PASS |
| 回归映射自检 | 537/537 |

三必跑组为Shanmen.0_0_10.Items、demo_map.ItemUseAndArmor及demo_map.P4.Hotbar。13项专项包含在完整根中，不另加到独立总数。原失败断言未放宽，拒绝/健康回滚变体均完成。所有测试执行同时核对结果、精确队列、原生退出和崩溃指标；未用旧阶段日志替代本轮结果。

## 4. 交接与边界

遵守[P阶段基线](../Process/P_STAGE_BASELINE_0_0_10.md)，交接限定为ItemSubsystem.cpp/h、既有PreparationAdapterTests、有限索引、本Report/Log共六路径。生产cpp36新增/9删除、头文件注释1换1、测试78新增，测试注册数不变。两份OverallReadiness用户修改及103原未跟踪文件保持不变，不暂存或提交。原始日志仅在本地，[Development Log](../Log/Dev.D.UE.0.0.10.P28.25.r0_log.md)记录13份原件路径/SHA，Git提交作为本阶段追溯基线。

[有限冻结索引](../Architecture/Dev.D.UE.0.0.10_FoundationClosure_Index.md)更新至P28.25；FZ-1/2仍开放。本次证明生成失败后原投影的同进程续清理，不替代完整World shutdown、全部生成失败原因、不变量失败分支的独立注入、所有终局组合或跨进程World恢复。不启动Editor UI、PIE、Standalone或产品程序，不做正式地图/资产、物理输入、UI、玩法数值、Smoke、Cook或Package。
